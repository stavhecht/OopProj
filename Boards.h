#pragma once

namespace Boards {
    constexpr int MAX_X = 80;
    constexpr int MAX_Y = 25;

    extern const char* const winBoard[MAX_Y];
    extern const char* const scoreBoard[MAX_Y];
    extern const char* const screenErrorBoard[MAX_Y];
    extern const char* const loseBoard[MAX_Y];
    extern const char* const riddleBoard[MAX_Y];
    extern const char* const manuBoard[MAX_Y];
    extern const char* const guideBoard[MAX_Y];
    extern const char* const gamePaused[MAX_Y];
}
